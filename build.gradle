allprojects {
    repositories {
        mavenCentral()
    }

    group('ru.otus')
    version('1.0-SNAPSHOT')
}

subprojects {
    apply plugin: 'java'

    test {
        useJUnitPlatform()
    }
}